spring.application.name=notification_microservice

server.port=3002
server.address=0.0.0.0

# Kafka Configuration
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.consumer.group-id=myGroup
kafka.topic.notifications=task-assignments

# Mail Configuration (Gmail) - Port 587 with STARTTLS
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:satrfond.api@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:webpnxgoxjgavxry}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=30000
spring.mail.properties.mail.smtp.timeout=30000
spring.mail.properties.mail.smtp.writetimeout=30000
spring.mail.properties.mail.debug=false

# Notification Settings
notification.sender.name=SATR System
